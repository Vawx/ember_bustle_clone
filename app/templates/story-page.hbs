{{outlet}}

<div class="container">
  <div class="col-xs-8 col-xs-offset-1">
    <h1>{{model.title}}</h1>
    <div class="row">
      <div class="well">
        <p>
          {{model.content}}
        </p>
      </div>
      <span>Written by: {{model.author}}, on: {{model.post_date}}</span>
    </div>
  </div>
</div>
